export const queryKeys = {
  destinations: ["destinations"],
  properties: (city) => ["properties", city],
  property: (id) => ["property", id],
  propertyReview: (id) => ["property-review", id],
  profile: (userId) => ["profile", userId],
  myBookingsPage: (page) => ["my-bookings", page],
  myBookingsForProperty: (userId, propertyId) => [
    "my-bookings-for-property",
    userId,
    propertyId,
  ],
  favorites: ["favorites"],
  favoritesList: ["favorites-list"],
  favoritesListPage: (page) => ["favorites-list", page],
  conversations: ["conversations"],
  conversationsPage: (page) => ["conversations", page],
  message: (conversationId, otherUserId) => [
    "message",
    conversationId,
    otherUserId,
  ],
  hostBookingsRoot: ["host-bookings"],
  hostBookings: (page) => ["host-bookings", page],
  myPropertiesList: (page) => ["my-properties-list", page],
  myPropertiesReviews: ["my-properties-reviews"],
  countries: ["countries"],
  cities: (countryId) => ["cities", countryId],
  amenities: (category) => ["amenities", category],
  users: ["admin-users"],
  adminUsers: (page, perPage, sortBy, sortDir) => [
    "admin-users",
    page,
    perPage,
    sortBy,
    sortDir,
  ],
  adminProperties: (page, perPage) => ["admin-properties", page, perPage],
  adminAmenities: ["admin-amenities"],
  adminBookings: ["admin-bookings"],
  adminBookingsList: (page, perPage, propertyId, order, sortBy, status) => [
    "admin-bookings",
    page,
    perPage,
    propertyId,
    order,
    sortBy,
    status,
  ],
  cart: ["cart"],
  search: (params) => ["search", params],
};
